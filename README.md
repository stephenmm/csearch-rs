# csearch-rs

Rust port of Google Code Search (cindex/csearch) with SIMD trigram extraction and parallel indexing/search

## Status
Started 2026-09-01. Work in progress.

## Setup
```bash
# (fill in how to run / install)
```

## Notes
- Lives on the E: drive at `E:\proj\csearch-rs` (`/mnt/e/proj/csearch-rs` in WSL).
- Backed up to a private GitHub repo. Push checkpoints with `/backup` (in Claude Code) or `git push`.

